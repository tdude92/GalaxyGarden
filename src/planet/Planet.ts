import { Rendered } from '../component/Rendered';
import { RigidBody } from '../component/RigidBody';

export abstract class Planet extends RigidBody implements Rendered {
    // TODO Planet base class
}